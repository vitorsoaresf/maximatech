import { StyleComponents } from "@interfaces/components";
import { theme } from "@styles";

export const CardImageStyle: StyleComponents = {
  maxWidth: "256px",
  maxHeight: "211px",
  borderRadius: theme.border.radius.md,
};
